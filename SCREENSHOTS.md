# チュートリアル撮影ガイド（改善提案用）

BimGenを開き、スタート画面の「実案件でBimGenを体験する」を押す。画面全体を同じブラウザサイズで撮影し、ブラウザのタブとOSのタスクバーは含めない。各行の操作を済ませた直後に撮る。

| 番号 | 撮る場面 | そこまでの操作 | 写り込ませるもの |
| --- | --- | --- | --- |
| ① 案件 | 開始直後、1/6 | まだ何も押さない | 左の「案件情報」の強調と下の説明 |
| ② 設計概要 | 2/6 | 「案件情報」を押す | 入力済みの案件概要と「入力済み」の案内 |
| ③ 図面 | 4/6 | 「入力済みの設計概要を確認」→「サンプル案件プランPDFを読み込む」 | 平面図のPDFを下敷きにした真上視点 |
| ④ なぞる | 4/6 | 青い頂点を1、2、3の順に押す。4はまだ押さない | 青い点と外形線が途中まで描かれた状態 |
| ⑤ 仮設配置 | 6/6 | 4の頂点を押し、左の「タワークレーン」をONにする。TCの点はまだ押さない | 3Dの建物とTC配置点の案内 |
| ⑥ 完成 | COMPLETE | TCの点を押す | 建物、TC、「基本的な施工計画が完成しました」 |

③は読み込みボタンを押した後に4/6へ自動進行するので、その画面を撮る。⑤でTCの点を押すと即座に完成する。途中で止めたい場合は、画面上部の「チュートリアルをスキップ」で現在のサンプル案件のまま自由操作に移れる。

PowerPointのステップ図は、**案件 → 設計概要 → PDF → 外形をなぞる → TCを配置 → 完成** の6コマで配置する。③から⑥の画像を少し大きく使うと、主操作が伝わる。PDFは説明用の架空サンプルであり、実案件の図面ではない。
